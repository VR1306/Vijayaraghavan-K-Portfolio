# Powers the AI FAQ chatbot (components/ChatBot.tsx via app/api/chat/route.ts).
# Get a key at https://console.anthropic.com/settings/keys
# Without this set, the chatbot automatically falls back to a rule-based
# FAQ matcher (lib/matchChatTopic.ts) — the site still works, just without AI.
ANTHROPIC_API_KEY=
